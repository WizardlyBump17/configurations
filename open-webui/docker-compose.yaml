services:
  open-webui:
    image: "ghcr.io/open-webui/open-webui:dev-slim"
    volumes:
      - "open-webui:/app/backend/data"
    environment:
      HF_HUB_OFFLINE: "1"
      HOST: "0.0.0.0"
      PORT: "8080"
    ports:
      - "8081:8080"

volumes:
  open-webui:

