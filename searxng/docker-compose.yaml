services:
  searxng:
    image: "docker.io/searxng/searxng:latest"
    volumes:
      - "./searxng/config/:/etc/searxng/"
      - "searxng:/var/cache/searxng/"
    ports:
      - "8082:8080"

volumes:
  searxng:
